<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Add Task</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: linear-gradient(135deg, #232526, #414345);
            font-family: "Segoe UI", sans-serif;
        }

        /* Glass Card */
        .task-card {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(12px);
            border-radius: 30px;
            padding: 45px;
            border: 1px solid rgba(255, 255, 255, 0.25);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.25);
            transition: 0.3s ease;
        }

        .task-card:hover {
            transform: translateY(-6px);
            box-shadow: 0 25px 55px rgba(0, 0, 0, 0.35);
        }

        /* Heading */
        h2 {
            font-weight: 700;
            color: #ffffff;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        /* Input */
        .task-input {
            height: 55px;
            border-radius: 14px;
            background: rgba(255, 255, 255, 0.92);
            border: none;
            font-size: 18px;
            padding-left: 18px;
            transition: 0.25s ease;
        }

        .task-input:focus {
            border: 2px solid #6a11cb;
            box-shadow: 0 0 14px rgba(106, 17, 203, 0.45);
        }

        /* Button */
        .task-btn {
            background: linear-gradient(45deg, #6a11cb, #2575fc);
            border: none;
            padding: 14px 45px;
            font-size: 19px;
            color: white;
            border-radius: 14px;
            font-weight: 600;
            transition: 0.25s ease;
        }

        .task-btn:hover {
            transform: scale(1.07);
            box-shadow: 0 8px 25px rgba(106, 17, 203, 0.5);
        }
    </style>

</head>

<body>

    <div class="container d-flex justify-content-center align-items-center min-vh-100">
        <div class="col-md-6">

            <div class="task-card">
                <h2 class="text-center mb-4">Add Task</h2>

                <form action="/addUser" method="post">

                    <div class="mb-4">
                        <input type="text" name="name" class="form-control task-input" placeholder="Enter your Name">
                    </div>
                    
                    <div class="mb-4">
                        <input type="text" name="age" class="form-control task-input" placeholder="Enter your Age...">
                    </div>

                    <div class="text-center">
                        <button class="task-btn">Add Task</button>
                    </div>

                </form>
            </div>

        </div>
    </div>

</body>

</html>
