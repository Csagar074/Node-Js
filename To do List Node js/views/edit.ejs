<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Edit Task</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            font-family: "Segoe UI", sans-serif;
        }

        .edit-card {
            background: rgba(255, 255, 255, 0.15);
            padding: 45px;
            border-radius: 25px;
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            box-shadow: 0px 15px 40px rgba(0, 0, 0, 0.25);
            border: 1px solid rgba(255, 255, 255, 0.25);
            transition: 0.3s;
            width: 90%;
            max-width: 520px;
            margin: auto;
            margin-top: 90px;
        }

        .edit-card:hover {
            transform: translateY(-6px);
            box-shadow: 0px 25px 60px rgba(0, 0, 0, 0.35);
        }

        h2 {
            color: #fff;
            font-weight: 700;
            letter-spacing: 1px;
        }

        .edit-input {
            height: 55px;
            font-size: 18px;
            border-radius: 14px;
            border: none;
            padding-left: 15px;
            margin-bottom: 18px;
            background: rgba(255, 255, 255, 0.85);
            transition: 0.25s ease;
        }

        .edit-input:focus {
            border: 2px solid #2575fc;
            box-shadow: 0 0 12px rgba(37, 117, 252, 0.55);
        }

        .update-btn {
            background: linear-gradient(45deg, #ff512f, #dd2476);
            padding: 13px 40px;
            color: #fff;
            border-radius: 14px;
            font-size: 20px;
            font-weight: 600;
            border: none;
            letter-spacing: 0.7px;
            transition: 0.25s ease;
        }

        .update-btn:hover {
            transform: scale(1.07);
            box-shadow: 0px 12px 28px rgba(255, 81, 47, 0.45);
        }
    </style>

</head>

<body>

    <div class="edit-card">
        <h2 class="text-center mb-4">Edit Task</h2>

        <form action="/updateUser" method="post">

            <input type="hidden" name="id" value="<%= user.id %>">

            <input type="text" name="name" class="form-control edit-input"
                value="<%= user.name %>" placeholder="Enter Your name">

            <input type="text" name="age" class="form-control edit-input"
                value="<%= user.age %>" placeholder="Enter Your age">

            <div class="text-center mt-3">
                <button class="update-btn">Update Task</button>
            </div>

        </form>
    </div>

</body>

</html>
