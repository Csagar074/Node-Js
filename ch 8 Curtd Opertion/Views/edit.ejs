<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update User Form</title>

    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #eef2f7;
            margin: 0;
            padding: 20px;
        }

        h1 {
            text-align: center;
            color: #1f2937;
            margin-bottom: 25px;
        }

        .container {
            width: 500px;
            margin: auto;
            background: #ffffff;
            padding: 25px;
            border-radius: 14px;
            box-shadow: 0 4px 18px rgba(0, 0, 0, 0.12);
        }

        table {
            width: 100%;
        }

        td {
            padding: 8px 0;
            font-size: 15px;
            color: #333;
        }

        input[type="text"],
        input[type="email"],
        input[type="password"],
        input[type="number"],
        textarea {
            width: 95%;
            padding: 10px;
            border: 1px solid #cbd5e1;
            border-radius: 8px;
            font-size: 14px;
            outline: none;
            transition: 0.2s;
        }

        textarea {
            height: 70px;
            resize: none;
        }

        input:focus,
        textarea:focus {
            border-color: #2563eb;
            box-shadow: 0 0 5px rgba(37, 99, 235, 0.3);
        }

        /* Update Button */
        input[type="submit"] {
            background: #0ea5e9;
            color: white;
            padding: 10px;
            width: 140px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 15px;
            transition: 0.3s;
        }

        input[type="submit"]:hover {
            background: #0284c7;
        }

        /* Go to Table Button */
        .goto-btn {
            margin-top: 20px;
            padding: 10px 25px;
            display: inline-block;
            color: white;
            background: #1e3a8a;
            border-radius: 8px;
            text-decoration: none;
            font-size: 16px;
            transition: 0.3s;
        }

        .goto-btn:hover {
            background: #3b5edb;
        }
    </style>

</head>

<body>

    <h1>Update Form</h1>

    <div class="container">

        <form method="POST" action="/updateUser" style="width: 100%;">
            <table cellspacing="5" cellpadding="5">

                <tr>
                    <input type="hidden" name="Id" value="<%= user.Id %>">
                    <td>Name :</td>
                    <td><input type="text" name="Name" required placeholder="Enter Your Name.." value="<%= user.Name %>"></td>
                </tr>

                <tr>
                    <td>Email :</td>
                    <td><input type="email" name="Email" required placeholder="Enter Your Email.." value="<%= user.Email %>"></td>
                </tr>

                <tr>
                    <td>Password :</td>
                    <td><input type="password" name="Password" required placeholder="Enter Your Password.." value="<%= user.Password %>"></td>
                </tr>

                <tr>
                    <td>Phone :</td>
                    <td><input type="number" name="Phone" required placeholder="Enter Your Phone Number.." value="<%= user.Phone %>"></td>
                </tr>

                <tr>
                    <td>Address :</td>
                    <td><textarea name="Address" required placeholder="Enter Your Address..."><%= user.Address %></textarea></td>
                </tr>

                <tr>
                    <td>Update</td>
                    <td><input type="submit" value="Update"></td>
                </tr>

            </table>
        </form>

    </div>

    <center>
        <a href="/" class="goto-btn">Go To Table</a>
    </center>

</body>

</html>
