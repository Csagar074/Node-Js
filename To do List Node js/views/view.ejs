<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Todo List View</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: linear-gradient(135deg, #2c3e50, #4ca1af);
            font-family: "Segoe UI", sans-serif;
        }

        h2 {
            color: #fff;
            font-weight: 700;
            text-shadow: 0px 4px 12px rgba(0, 0, 0, 0.3);
            letter-spacing: 1px;
            text-align: center;
            margin-bottom: 40px;
        }

        /* Card / List Item */
        .todo-card {
            background: rgba(255, 255, 255, 0.18);
            border-radius: 20px;
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.25);
            padding: 18px 22px;
            margin-bottom: 14px;
            transition: 0.3s ease;
            box-shadow: 0px 10px 25px rgba(0, 0, 0, 0.18);
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .todo-card:hover {
            transform: scale(1.02);
            box-shadow: 0px 15px 35px rgba(0, 0, 0, 0.25);
        }

        .task-number {
            color: #ffffff;
            font-size: 20px;
            font-weight: 700;
            padding: 8px 14px;
            background: linear-gradient(45deg, #8e2de2, #4a00e0);
            border-radius: 12px;
            box-shadow: 0px 8px 20px rgba(142, 45, 226, 0.5);
        }

        .task-text {
            font-size: 20px;
            color: #ffffff;
            font-weight: 600;
            width: 200px;
            text-align: center;
        }

        .task-age {
            font-size: 20px;
            color: #ffe8ff;
            font-weight: 600;
            width: 150px;
            text-align: center;
        }

        /* Buttons */
        .btn-edit,
        .btn-delete {
            border: none;
            padding: 8px 20px;
            border-radius: 14px;
            font-weight: 600;
            letter-spacing: .5px;
            transition: 0.25s ease;
            text-decoration: none;
        }

        .btn-edit {
            background: #0b8fc3;
            color: rgb(23, 21, 21);
        }

        .btn-edit:hover {
            background: #26ce1a;
            transform: scale(1.07);
        }

        .btn-delete {
            background: #ff4757;
            color: rgb(35, 34, 34);
        }

        .btn-delete:hover {
            background: #e84118;
            transform: scale(1.07);
        }

        /* Add User Button */
        .add-btn {
            background: linear-gradient(45deg, #8e2de2, #4a00e0);
            color: white;
            font-size: 20px;
            padding: 13px 35px;
            border-radius: 14px;
            text-decoration: none;
            transition: 0.3s ease;
            border: none;
        }

        .add-btn:hover {
            transform: scale(1.05);
            box-shadow: 0px 15px 30px rgba(142, 45, 226, 0.4);
        }
    </style>

</head>

<body>

    <div class="container py-5">

        <h2>To-Do List</h2>

        <% allUser.map((user, index)=> { %>
        <div class="todo-card">

            <!-- Number -->
            <div class="task-number">
                <%= index + 701 %>
            </div>

            <!-- Name -->
            <div class="task-text">
                <%= user.name %>
            </div>

            <!-- Age -->
            <div class="task-age">
                <%= user.age %>
            </div>

            <!-- Buttons -->
            <div>
                <a href="/editUser?id=<%= user.id %>" class="btn-edit me-2">Edit</a>
                <a href="/deleteUser?id=<%= user.id %>" class="btn-delete">Delete</a>
            </div>

        </div>
        <% }) %>

        <div class="text-center mt-5">
            <a href="/addUser" class="add-btn">Add User</a>
        </div>

    </div>

</body>

</html>
