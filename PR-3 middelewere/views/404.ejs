<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 Error</title>

    <!-- Fonts & Icons -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.1.0/fonts/remixicon.css" rel="stylesheet">

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Inter", sans-serif;
        }

        body {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            background: linear-gradient(135deg, #1b1b1b, #2b003a);
            color: #fff;
        }

        /* Container */
        .error-container {
            width: 100%;
            max-width: 450px;
        }

        /* Card */
        .error-card {
            background: rgba(255, 255, 255, 0.06);
            backdrop-filter: blur(12px);
            border-radius: 20px;
            padding: 35px;
            text-align: center;
            box-shadow: 0px 8px 25px rgba(0, 0, 0, 0.3);
            animation: fadeUp 0.6s ease;
        }

        @keyframes fadeUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Icon */
        .error-icon i {
            font-size: 70px;
            color: #ff4b5c;
            background: rgba(255, 75, 92, 0.15);
            padding: 20px;
            border-radius: 50%;
        }

        /* Headings */
        h1 {
            font-size: 30px;
            margin-top: 15px;
            font-weight: 700;
            color: #ff4b5c;
        }

        p {
            margin-top: 8px;
            font-size: 16px;
            opacity: 0.8;
        }

        /* Info Box */
        .error-details {
            background: rgba(255, 255, 255, 0.08);
            padding: 15px;
            border-radius: 12px;
            margin-top: 20px;
        }

        .restriction-info {
            display: flex;
            align-items: center;
            gap: 10px;
            color: #fff;
            font-size: 15px;
        }

        .restriction-info i {
            font-size: 22px;
            color: #ffd369;
        }

        /* Buttons */
        .error-actions {
            margin-top: 25px;
        }

        .retry-btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 12px 20px;
            text-decoration: none;
            background: #ff4b5c;
            color: #fff;
            font-weight: 600;
            border-radius: 10px;
            font-size: 16px;
            transition: 0.3s ease;
        }

        .retry-btn:hover {
            background: #d73848;
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(255, 75, 92, 0.3);
        }

        .retry-btn i {
            font-size: 20px;
        }
    </style>
</head>

<body>
    <div class="error-container">
        <div class="error-card">
            <div class="error-icon">
                <i class="ri-shield-cross-line"></i>
            </div>

            <h1>Access Denied</h1>
            <p>You must be 18 years or older to access this Website</p>

            <div class="error-details">
                <div class="restriction-info">
                    <i class="ri-information-line"></i>
                    <span>This website contains educational content intended for adults</span>
                </div>
            </div>

            <div class="error-actions">
                <a href="/" class="retry-btn">
                    <i class="ri-refresh-line"></i>
                    Try Again
                </a>
            </div>
        </div>
    </div>
</body>

</html>
