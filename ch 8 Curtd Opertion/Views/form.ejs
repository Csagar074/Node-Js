<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form Page</title>

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Inter", sans-serif;
        }

        body {
            background: linear-gradient(135deg, #e3f2fd, #ffffff);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 30px;
        }

        h1 {
            margin-bottom: 25px;
            font-size: 32px;
            color: #0d47a1;
            font-weight: 700;
            letter-spacing: 1px;
        }

        .form-card {
            background: #ffffff;
            width: 430px;
            padding: 30px 35px;
            border-radius: 18px;
            box-shadow: 0px 10px 25px rgba(0, 0, 0, 0.12);
            animation: fadeIn 0.6s ease;
        }

        table {
            width: 100%;
        }

        td {
            padding: 10px 0;
            font-size: 16px;
            color: #333;
            font-weight: 500;
        }

        input,
        textarea {
            width: 100%;
            padding: 12px 14px;
            font-size: 15px;
            border: 1.5px solid #d0d0d0;
            border-radius: 10px;
            outline: none;
            transition: 0.25s;
            background: #fafafa;
        }

        input:focus,
        textarea:focus {
            border-color: #2196f3;
            background: #ffffff;
            box-shadow: 0px 0px 8px rgba(33, 150, 243, 0.25);
        }

        textarea {
            height: 80px;
            resize: none;
        }

        .btn-submit {
            width: 100%;
            padding: 12px 12px;
            background: #2196f3;
            border: none;
            font-size: 17px;
            color: white;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            margin-top: 10px;
            transition: 0.25s;
        }

        .btn-submit:hover {
            background: #0b7dda;
            transform: translateY(-2px);
            box-shadow: 0px 6px 15px rgba(33, 150, 243, 0.3);
        }

        .btn-submit:active {
            transform: translateY(1px);
        }

        a {
            margin-top: 20px;
            font-size: 18px;
            text-decoration: none;
            color: #0d47a1;
            font-weight: 600;
        }

        a:hover {
            text-decoration: underline;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(15px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>

</head>

<body>

    <h1>User Form</h1>

    <div class="form-card">
        <form method="POST" action="/addUser">
            <table>
                <tr>
                    <td>Name</td>
                </tr>
                <tr>
                    <td><input type="text" name="Name" required placeholder="Enter your full name..."></td>
                </tr>

                <tr>
                    <td>Email</td>
                </tr>
                <tr>
                    <td><input type="email" name="Email" required placeholder="Enter your email..."></td>
                </tr>

                <tr>
                    <td>Password</td>
                </tr>
                <tr>
                    <td><input type="password" name="Password" required placeholder="Enter your password..."></td>
                </tr>
                <tr>
                    <td>Phone</td>
                </tr>
                <tr>
                    <td><input type="number" name="Phone" required placeholder="Enter phone number..."></td>
                </tr>

                <tr>
                    <td>Address</td>
                </tr>
                <tr>
                    <td><textarea name="Address" required placeholder="Enter address..."></textarea></td>
                </tr>

                <tr>
                    <td><input class="btn-submit" type="submit" value="Add User"></td>
                </tr>
            </table>
        </form>
    </div>

    <a href="/addUserPage">⬅ Back to Table Page</a>

</body>

</html>
